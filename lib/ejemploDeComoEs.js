// import moongose from 'mongoose';

                 // con esta constante nosostros traemos la variable de entorno con usuario y contraseña para conectarnos a la base de datos
                 // el "process.env" es para evitar q se suba al repositorio, como un git ignore.

// const URI_MONGODB = process.env.URI_MONGODB

// const conectarDataBase = async () => {
//     try {
//         await moongose.connect(URI_MONGODB, {
                    //       useNewUrlParser: true,
                    //       useUnifiedTopology: true,
                    //       bufferCommands: false,
                    //       bufferMaxEntries: 0,
                    //       useFindAndModify: false,
                    //       useCreateIndex: true,
// })
//     } catch (error) {
//         process.exit(1);
//     }
// }

// export default conectarDataBase;